# Lessons 1-4 need only these three. No gym, no MuJoCo, no OpenGL, no simulator.
torch>=2.0
numpy>=1.24
matplotlib>=3.6

# Lessons 5-6 add real environments. Install these only when you get there:
#     pip install gymnasium mujoco
# and on a headless machine, before either is imported:
#     export MUJOCO_GL=egl
# gymnasium>=1.0
# mujoco>=3.0
